image: node:15.12-alpine3.13

stages:
 - test
 - deploy

test:
 stage: test
 before_script:
   - yarn install
 script:
  - yarn build
 except:
  - main
 only:
   - main

pages:
 stage: deploy
 before_script:
   - yarn install
 script:
  - yarn build
  - mv ./build ./public
 artifacts:
  paths:
   - public
 only:
   - main
